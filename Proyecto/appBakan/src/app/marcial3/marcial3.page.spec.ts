import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Marcial3Page } from './marcial3.page';

describe('Marcial3Page', () => {
  let component: Marcial3Page;
  let fixture: ComponentFixture<Marcial3Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(Marcial3Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
