import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Marcial4Page } from './marcial4.page';

describe('Marcial4Page', () => {
  let component: Marcial4Page;
  let fixture: ComponentFixture<Marcial4Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(Marcial4Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
