import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Martin3Page } from './martin3.page';

describe('Martin3Page', () => {
  let component: Martin3Page;
  let fixture: ComponentFixture<Martin3Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(Martin3Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
