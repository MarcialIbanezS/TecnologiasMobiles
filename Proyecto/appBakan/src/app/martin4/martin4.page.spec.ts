import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Martin4Page } from './martin4.page';

describe('Martin4Page', () => {
  let component: Martin4Page;
  let fixture: ComponentFixture<Martin4Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(Martin4Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
