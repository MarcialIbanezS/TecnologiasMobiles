import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Martin6Page } from './martin6.page';

describe('Martin6Page', () => {
  let component: Martin6Page;
  let fixture: ComponentFixture<Martin6Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(Martin6Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
