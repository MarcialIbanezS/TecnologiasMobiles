import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Matias5Page } from './matias5.page';

describe('Matias5Page', () => {
  let component: Matias5Page;
  let fixture: ComponentFixture<Matias5Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(Matias5Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
