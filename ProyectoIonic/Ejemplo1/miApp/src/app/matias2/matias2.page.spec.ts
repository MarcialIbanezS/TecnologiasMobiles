import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Matias2Page } from './matias2.page';

describe('Matias2Page', () => {
  let component: Matias2Page;
  let fixture: ComponentFixture<Matias2Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(Matias2Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
