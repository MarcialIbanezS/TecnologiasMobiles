import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Matias3Page } from './matias3.page';

describe('Matias3Page', () => {
  let component: Matias3Page;
  let fixture: ComponentFixture<Matias3Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(Matias3Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
