import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Matias4Page } from './matias4.page';

describe('Matias4Page', () => {
  let component: Matias4Page;
  let fixture: ComponentFixture<Matias4Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(Matias4Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
