import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Matias6Page } from './matias6.page';

describe('Matias6Page', () => {
  let component: Matias6Page;
  let fixture: ComponentFixture<Matias6Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(Matias6Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
