import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Matias7Page } from './matias7.page';

describe('Matias7Page', () => {
  let component: Matias7Page;
  let fixture: ComponentFixture<Matias7Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(Matias7Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
